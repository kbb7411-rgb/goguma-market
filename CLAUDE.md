# CLAUDE.md — 고구마마켓 작업 규칙

이 폴더는 **개발 공부용** 프로젝트입니다. 사용자는 비개발자이며,
"빨리 완성된 코드를 받는 것"보다 **하나씩 이해하며 만들어 가는 것**이 목적입니다.

---

## 1. 대화 방식 (가장 중요)

### 쉬운 말로 설명하기
- 개발 용어가 나오면 **반드시 한 줄 풀이를 붙입니다.**
  - ❌ "RLS 정책을 추가했습니다"
  - ✅ "RLS(내 데이터는 나만 보이게 하는 DB 잠금장치) 규칙을 추가했습니다"
- 영어 약어(SSR, ORM, migration, hydration…)는 처음 쓸 때 괄호로 풀어 씁니다.
- 비유를 적극적으로 씁니다. (쿠키 = 손목 도장, 미들웨어 = 입구 검표원 등)

### 진행 상황을 자주 알리기
- 작업을 시작할 때: **"지금부터 ○○을 만듭니다"**
- 중간중간: **"지금 △△ 파일을 고치는 중입니다"**
- 끝났을 때: **"○○ 완료. 화면에서 □□가 보이면 성공입니다"**
- 파일을 3개 이상 건드리는 작업이면 먼저 **할 일 목록을 짧게 보여주고** 시작합니다.

### 시킨 것만, 한 단계씩
- 사용자가 요청한 범위 **딱 그만큼만** 만듭니다.
- "이것도 같이 만들까요?" 하는 앞서가기 금지. 대신 **끝에 한 줄로 다음 단계 제안**만 합니다.
- 리팩터링·구조 개선·라이브러리 추가는 **먼저 물어보고** 진행합니다.

### 사람이 할 일은 따로 정리
- Claude가 할 수 있는 건 **끝까지 다 합니다.** (파일 작성, DB 마이그레이션, 서버 실행, 화면 확인)
- 사람만 할 수 있는 일(대시보드 클릭, 결제, 도메인 연결 등)은 마지막에 이렇게 안내합니다:

  ```
  👤 사용자가 직접 할 일
  1. Supabase 대시보드 → Authentication → ... 접속
  2. 'Confirm email' 스위치를 끕니다
  3. 다 하셨으면 "했어요" 라고 말씀해 주세요
  ```
  화면 이름·버튼 이름을 **보이는 그대로** 적고, 왜 필요한지도 한 줄 덧붙입니다.

### 다 만들면 바로 열어서 보여주기
- 코드 수정이 끝나면 **개발 서버를 직접 띄우고**, 브라우저로 해당 화면을 열고,
  **스크린샷까지 확인한 뒤** 결과를 보고합니다.
- "직접 확인해 보세요"라고 떠넘기지 않습니다.

---

## 2. 이 프로젝트가 무엇인가

당근마켓을 참고한 중고거래 사이트 **고구마마켓**.

| 단계 | 내용 | 상태 |
| --- | --- | --- |
| 1 | 회원가입 / 로그인 / 로그아웃, 프로필 | ✅ 완료 (2026-09-21) |
| 2 | 거래 글 CRUD — 등록·목록·상세·수정·삭제, 이미지 업로드, 판매 상태, 검색/카테고리 | ✅ 완료 (2026-09-21) |
| 3 | 찜하기 (하트) + 카드에 찜·채팅 아이콘 표시 | ✅ 완료 |
| 4 | 이웃과 1:1 채팅 | ⬜ |
| 5 | 마이페이지 (내 판매 목록, 프로필 수정) | ⬜ |

한 번에 한 단계씩. 단계를 끝내면 README.md 의 진행 표도 같이 갱신합니다.

---

## 3. 기술 구성

- **Next.js 15 (App Router) + TypeScript** — 화면과 서버 로직
- **Tailwind CSS v4** — 디자인. 색상 토큰은 `app/globals.css` 의 `@theme`
- **Supabase** — 회원(Auth) + 데이터베이스 + 파일 저장소
- **Vercel** — 배포 (고구마마켓 전용 프로젝트 / 가계부와 별도 도메인)

### 폴더 지도
```
app/                 화면(페이지)과 서버 액션
components/          재사용 UI 조각
lib/supabase/        Supabase 연결 코드 (브라우저용/서버용/미들웨어용)
middleware.ts        모든 요청 전에 로그인 세션을 갱신
supabase/migrations/ 실제로 적용한 SQL 을 기록해 두는 곳
```

---

## 4. Supabase 규칙 ⚠️

**가계부 앱과 Supabase 프로젝트(`vrkgrwpfuskcjqbdtihz`)를 함께 씁니다.**

- 새로 만드는 테이블·함수·트리거·스토리지 버킷은 **반드시 `ggm_` 접두사**
  (예: `ggm_profiles`, `ggm_products`)
- 가계부의 `public.entries` 테이블은 **읽지도 고치지도 않습니다.**
- 테이블을 새로 만들면 **RLS(행 단위 접근 제한)를 반드시 켜고** 정책까지 같이 만듭니다.
- 적용한 SQL 은 `supabase/migrations/NNNN_설명.sql` 로 **파일에도 남깁니다.**
  (나중에 무엇을 했는지 되짚어 보기 위함)
- MCP 도구로 DB를 바꿀 때는 **무엇을 바꾸는지 먼저 한국어로 설명**하고 진행합니다.

---

## 5. 개발 서버 실행 방법 ⚠️

**이 PC에서는 `npm` 이 PATH에 없습니다.** (Node는 winget 사용자 설치라 경로가 따로 있음)

- 서버 실행은 **반드시 `preview_start` 도구 + `.claude/launch.json` 의 `goguma-dev`** 로 합니다.
  (`node.exe` 절대경로가 들어 있어 그냥 동작합니다.)
- 터미널에서 `npm install` 등이 꼭 필요하면 PowerShell에서 경로를 앞에 붙입니다:

  ```powershell
  $env:Path = "C:\Users\student\AppData\Local\Microsoft\WinGet\Packages\OpenJS.NodeJS.LTS_Microsoft.Winget.Source_8wekyb3d8bbwe\node-v24.19.0-win-x64;" + $env:Path
  ```
- Claude Code 앱을 재시작한 뒤에는 그냥 `npm` 이 될 수도 있으니 `node -v` 로 먼저 확인합니다.

---

## 6. 코드를 쓸 때

- **주석을 한국어로, 넉넉히.** "이 줄이 무슨 일을 하는지"를 초보자 기준으로 적습니다.
  (공부가 목적이므로 주석이 곧 교재입니다.)
- 파일 하나가 길어지면 나누되, **폴더 구조는 위 지도를 유지**합니다.
- 새 라이브러리 설치는 **먼저 물어봅니다.** 기본은 지금 있는 것만으로 해결.
- 비밀 키는 `.env.local` 에만. 코드에 직접 박지 않습니다.
- 커밋은 사용자가 요청할 때만. 커밋 메시지는 한국어로 `N단계: 내용` 형식.

---

## 7. 마무리 체크리스트

작업을 끝냈다고 말하기 전에:

- [ ] 개발 서버를 띄워 화면이 실제로 뜨는지 확인했는가
- [ ] 브라우저 콘솔에 빨간 오류가 없는가
- [ ] 사용자가 직접 할 일이 있다면 따로 정리해 안내했는가
- [ ] README.md 의 진행 표를 갱신했는가
- [ ] 다음에 무엇을 하면 좋을지 한 줄로 제안했는가
